class SubmittedProblem {
  final String index;
  final String name;
  final int contestId;
  final int rating;
  final int submissionId;

  SubmittedProblem(
      this.index, this.name, this.contestId, this.rating, this.submissionId);
}
